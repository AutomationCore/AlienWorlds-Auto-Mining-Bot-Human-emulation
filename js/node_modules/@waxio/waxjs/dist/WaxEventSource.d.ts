export declare class WaxEventSource {
    private waxSigningURL;
    constructor(waxSigningURL?: string);
    openEventSource(url: string, message?: any, win?: Window): Promise<any>;
    onceEvent(source: Window, origin: string, action: (e: any) => void): Promise<unknown>;
    private timeout;
}
